<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:map="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_height="match_parent"
    android:layout_width="match_parent">
    <fragment
        android:id="@+id/map"
        android:name="com.google.android.gms.maps.SupportMapFragment"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="50dp"
        tools:context="DriversMapActivity"
        tools:layout="@layout/activity_drivers_map" />

    <Button
        android:id="@+id/btnCustomerLogout"
        android:layout_width="190dp"
        android:layout_height="wrap_content"
        android:layout_alignParentEnd="true"
        android:layout_alignParentTop="true"
        android:text="Logout"
        android:textAlignment="center"
        android:backgroundTint="@color/redPrimaryDark"/>


    <Button
        android:id="@+id/btnBookRide"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="620dp"
        android:text="Book a Ride"
        android:textColor="@color/redPrimary"
        android:backgroundTint="@color/yellowPrimary" />


</RelativeLayout>